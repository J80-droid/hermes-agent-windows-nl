# Upstream pytest known failures (nodeids). Comments start with #.
# Used by summarize_pytest_junit.py to flag "new" vs "known" failures.
# Populate after first RUN_PYTEST_UPSTREAM -ReportOnly run; not a release gate.
