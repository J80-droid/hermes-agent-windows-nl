{
  "name": "codebase-viz",
  "private": true,
  "version": "2.4.0",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
